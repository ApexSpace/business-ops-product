/** Gift cards list + detail workspace (full-bleed below topbar, no shell content scroll). */
export function isGiftCardsWorkspacePath(pathname: string): boolean {
  return pathname === "/business/gift-cards";
}
