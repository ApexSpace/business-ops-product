/** Packages list + detail workspace (full-bleed below topbar, no shell content scroll). */
export function isPackagesWorkspacePath(pathname: string): boolean {
  return pathname === "/business/packages";
}
