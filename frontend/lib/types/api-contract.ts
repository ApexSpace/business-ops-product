/**
 * Re-export generated OpenAPI types for gradual migration.
 * Feature code should prefer `features/<domain>/types` until each domain migrates.
 */
export type { paths, components } from "@business-automation/api-contract";
