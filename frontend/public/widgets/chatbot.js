/**
 * @deprecated Load from BACKEND_PUBLIC_URL/widgets/chatbot.js — see chatbot embed API.
 */
(function () {
  console.warn(
    "[chatbot] Use the embed code from Settings → Chatbots (backend /widgets/chatbot.js).",
  );
})();
