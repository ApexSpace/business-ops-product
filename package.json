{
  "name": "business-automation",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"wait-on tcp:3000 && npm run dev:frontend\"",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
